import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bonfire.pizzeria',
  appName: 'Bonfire Pizzeria Multan',
  webDir: 'dist'
};

export default config;
